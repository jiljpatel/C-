#include<iostream>

using namespace std;
int main()
{ int *p,i,a;
int num[a];
cout<<"Enter the elements : ";
cin>>num[a];
p = num;
cout<<"You have entered : ";

{ cout<<""<<num[a];
p++;
}

return 0;
}
